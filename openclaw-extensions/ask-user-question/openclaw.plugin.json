{
  "id": "ask-user-question",
  "name": "AskUserQuestion",
  "description": "Structured user confirmation tool for NukemAI desktop.",
  "contracts": {
    "tools": [
      "AskUserQuestion"
    ]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "callbackUrl": {
        "type": "string",
        "description": "NukemAI HTTP callback URL for approval requests"
      },
      "secret": {
        "type": "string",
        "description": "Shared secret for callback authentication"
      }
    }
  }
}
